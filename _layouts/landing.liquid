<!doctype html>
<html lang="{{ site.lang }}">
  <head>
    {% include head.liquid %}
  </head>
  <body class="landing-page">
    <main class="landing-shell" role="main">
      {{ content }}
    </main>
  </body>
</html>
